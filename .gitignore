# 账号配置（本地账号名 / profile 映射，按需求不纳入版本库）
jd_accounts.json

# 浏览器 profile 目录（真实 Chrome 用户数据，体积大且含隐私，禁止提交）
chrome_jd/
chrome_jd_*/
jd_profile/

# Python 缓存
__pycache__/
*.pyc

# 运行日志
*.log

# 临时文件
*.tmp

# 截图等二进制
*.png

# 推送配置（含 Server 酱密钥，禁止提交）
jd_notify.json

# 本地工作记忆 / 诊断日志 / 临时脚本（含记忆与运行日志，勿入库）
.codebuddy/
